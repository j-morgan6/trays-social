#!/usr/bin/env bash

REPO=$(git rev-parse --show-toplevel)

# Tracking SDK audit — fails the push if any new tracking SDK appears
"$REPO/scripts/audit-no-tracking-sdks.sh" || exit $?

# iOS Release build
exec "$REPO/scripts/pre-push-ios.sh" "$@"
